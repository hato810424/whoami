services:
  whoami-dist:
    build:
      context: .
      args:
        GIT_COMMIT: ${GIT_COMMIT:-}
    volumes:
      - ./dist:/out
    # dist 生成だけ行う（nginx はホスト側で配信している前提）
    command: sh -c "rm -rf /out/* && cp -a /dist/. /out/"

